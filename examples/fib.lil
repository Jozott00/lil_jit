fn fib(n) {
    if n < 1: return 0
    if n < 3: return 1
    return fib(n - 1) + fib(n - 2)
}

fn main() {
    for let i = 0; i < 20; i = i + 1 {
        show(i)
        showtext(": ")
        showln(fib(i))
    }
}